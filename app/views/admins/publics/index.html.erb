一般ユーザ

<table>
  <tr>
    <th>No.</th>
    <th>ユーザー名</th>
    <th>登録日</th>
    <th>ステータス</th>
    <th>詳細</th>
  </tr>

  <% @publics.each do |publics| %>
  <tr>
    <td><%= publics.id %></td>
    <td><%= publics.name %></td>
    <td><%= l publics.created_at %></td>
    <td><%= publics.is_active %></td>
    <td>詳細</td>
  </tr>
  <% end %>
</table>